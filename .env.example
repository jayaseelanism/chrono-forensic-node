# Example environment variables for local development
# Copy this file to .env.local and add your real secrets. NEVER commit real keys.

# Gemini / Google GenAI API key (server-side only)
GEMINI_API_KEY=

# Allowed origin for the AI proxy server (set to your dev frontend origin)
ALLOWED_ORIGIN=http://localhost:3000

# Optional: port for the AI server
PORT=4000

# Optional server API key: if set, clients must present this in the X-API-KEY header
SERVER_API_KEY=

# TLS certificate and key (optional). If provided, server will run HTTPS.
# Provide absolute paths to PEM files.
TLS_CERT_PATH=/path/to/fullchain.pem
TLS_KEY_PATH=/path/to/privkey.pem

# Optional: Vault configuration (if you use HashiCorp Vault to store secrets)
# VAULT_ADDR should include protocol, e.g. https://vault.mycompany.internal:8200
VAULT_ADDR=
VAULT_TOKEN=
VAULT_SECRET_PATH=secret/data/chrono
